export const LiveFlickConfig = {
    cooldownTime: 5000, // 5 seconds cooldown
    aiCheckInterval: 500, // AI checks every 500ms
    aiFlickDelay: 1000, // AI waits 1s after cooldown before flicking
};
